#include "main.h"
#include <stdlib.h>
#include <stdio.h>
/**
 * main - check the code
 * @argc: int to be checked
 * @argv: char to be checked
 * Return: Always 0
 */
int main(int argc, char *argv[])
{
	(void)argc;
	printf("%s\n", argv[0]);
	return (0);
}
