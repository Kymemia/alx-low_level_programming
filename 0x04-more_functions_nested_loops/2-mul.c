#include "main.h"

/**
 * mul - multiplies two integers
 * @shrek: first integer to multiply
 * @donkey: second integer to multiply
 * Return: Always 0 (Success)
 */

int mul(int shrek, int donkey)
{
	return (shrek * donkey);
}
