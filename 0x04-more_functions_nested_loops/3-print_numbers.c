#include "main.h"

/**
 * print_numbers - prints numbers in a range
 * @rR: int to be printed
 * Return: Always 0 (Success)
 */

void print_numbers(void)
{
	int rR;
	for (rR >= 0; rR <= 9; rR++)
		_putchar(rR);
}
